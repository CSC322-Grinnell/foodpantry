<!-- Example row of columns -->
<div class="row">
  <div class="col-md-3">

  </div>
  <div class="col-md-6">
    <h1>Food Items in Need</h1>

<table class="table table-striped">
  <tr>
    <th>Name</th>
  </tr>

<% @needed_food_items.each do |food_item| %>
  <tr>
    <td style="font-size:20px;"><%= food_item.name %></td>
  </tr>
<% end %>
</table>

<h1>Accepted Food Items</h1>

<table class="table table-striped">
  <tr>
    <th>Name</th>
  </tr>

<% @food_items.each do |food_item| %>
  <tr>
    <td style="font-size:20px;"><%= food_item.name %></td>
  </tr>
<% end %>
</table>

<br />
  </div>
</div>

